apiVersion: apps/v1
kind: Deployment
metadata:
  name: todox-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todox-api
  template:
    metadata:
      labels:
        app: todox-api
    spec:
      containers:
        - name: todox-api
          image: todox-api:local
          imagePullPolicy: Never
          ports:
            - containerPort: 5287
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Development"
            - name: ASPNETCORE_URLS
              value: "http://+:5287"
t
            - name: ConnectionStrings__PosgreSQL
              valueFrom:
                secretKeyRef:
                  name: todox-api-secrets
                  key: ConnectionStrings__PosgreSQL

            - name: Jwt__Secret
              valueFrom:
                secretKeyRef:
                  name: account-jwt-secret
                  key: Jwt__Secret
